---
extends: relaxed

rules:
  line-length: disable
  trailing-spaces: enable
  new-line-at-end-of-file: enable
  new-lines:
    type: unix
  indentation: disable
  key-duplicates: disable
  empty-lines: enable
  colons: disable
  commas: disable

ignore: |
  /test-api-source-repo/.github/ISSUE_TEMPLATE/config.yml
  /test-api-source-repo/addons/**/*/
